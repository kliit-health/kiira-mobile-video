const initialState = {};

const addNewProfileReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};

export default addNewProfileReducer;
