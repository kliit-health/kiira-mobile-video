import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 20,
        height: 240,
    },
    background: {
        position: 'absolute',
        top: 20,
        left: 20,
        width: '100%',
        height: 200,
        borderRadius: 8,
    },
});
