const initialState = {};
const agreeToTerms = (state = initialState, action) => {
    switch (action.type) {
        default:
            return {
                ...state,
            };
    }
};
export default agreeToTerms;
