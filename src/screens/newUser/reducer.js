const initialState = {};
const agreeToTermsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};
export default agreeToTermsReducer;
