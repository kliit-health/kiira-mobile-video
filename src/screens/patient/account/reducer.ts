const initialState = {};

const account = (state = initialState, action) => {
    switch (action.type) {
        default:
            return {
                ...state,
            };
    }
};
export default account;
