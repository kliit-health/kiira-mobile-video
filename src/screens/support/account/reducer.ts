const initialState = {};

const accountExpert = (state = initialState, action) => {
  switch (action.type) {
    default:
      return {
        ...state,
      };
  }
};
export default accountExpert;
